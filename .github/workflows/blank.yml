name: Deployment

on:
  push:
    branches:
      - production

jobs:
  plan:
    runs-on: ubuntu-latest
    steps:
      - name: deploy
        run: echo "Plan!"
        
  deployment:
    runs-on: ubuntu-latest
    environment: production
    steps:
      - name: deploy
        run: echo "Deploy!"
